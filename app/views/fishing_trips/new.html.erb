<% provide(:title, 'Fishing Trip') %>
<% provide(:button_text, 'Add Fishing Trip') %>
<div class="row">
  <div class="col-md-6 col-md-offset-3">
    <%= form_for(@fishing_trip) do |f| %>
      <%= f.label :name %>
      <%= f.text_field :name %>
    <% end %>
  </div>
</div>